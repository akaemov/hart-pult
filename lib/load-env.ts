/// Загрузка .env для скриптов, запускаемых напрямую через tsx.
///
/// Импортируется ПЕРВОЙ строкой скрипта: модули верхнего уровня выполняются
/// в порядке импортов, и lib/prisma.ts читает DATABASE_URL уже при загрузке.
/// Если положить загрузку в конец файла, она случится после — и клиент
/// упадёт на пустой переменной. Next.js читает .env сам, ему это не нужно.
try {
  process.loadEnvFile(".env");
} catch {
  // Файла может не быть — тогда переменные берутся из окружения.
}

export {};
